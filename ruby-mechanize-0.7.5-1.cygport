ORIG_PN="mechanize"
inherit ruby

DESCRIPTION="Ruby automated website interaction library"
HOMEPAGE="http://mechanize.rubyforge.org/mechanize/"
SRC_URI="mirror://rubyforge/${ORIG_PN}/${ORIG_PN}-${PV}.tgz"

src_install() {
	cd ${B}
	dodir ${RUBY_SITELIB}
	cp -rL lib/* ${D}${RUBY_SITELIB}
}
